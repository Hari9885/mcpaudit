name: ci
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: "22" }
      - run: npm ci
      - run: npm run typecheck
      - run: npm run build
      - run: npm test
      - name: self-dogfood (fixtures must grade as expected)
        run: |
          npm run build
          node dist/cli.js --stdio "node fixtures/good/server.ts" | grep -E "\(A\)"
          node dist/cli.js --stdio "node fixtures/evil/server.ts" | grep -E "\(F\)"
      - name: badge output is a valid shields endpoint
        run: |
          node dist/cli.js --stdio "node fixtures/evil/server.ts" --badge badge.json > /dev/null
          grep -q '"schemaVersion":1' badge.json
          grep -q '45/100 (F)' badge.json
